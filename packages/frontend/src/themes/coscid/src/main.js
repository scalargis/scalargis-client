import React from 'react';
import "@fontsource/saira"; // Defaults to weight 400.
import ThemeDefault from '../../default/main'
import './style.css';

const Theme = () => (<React.Fragment></React.Fragment>);

export default Theme;